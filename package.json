{
  "name": "odata-v4-filter-parser",
  "version": "0.0.1",
  "description": "Parse OData $filter syntax into MongoDB syntax in JS",
  "main": "index.js",
  "scripts": {
    "build": "pegjs -o index.js ./src/parser.pegjs",
    "test": "jest"
  },
  "author": "ljqx",
  "license": "MIT",
  "devDependencies": {
    "jest": "^23.0.1",
    "pegjs": "^0.10.0"
  }
}
